{
  "name": "super8-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "start-local": "npm run build-frontend:win && node server.js",
    "build-frontend:win": "cd ..\\frontend && npm install && npm run build && cd ..\\backend && if exist dist rmdir /s /q dist && xcopy ..\\frontend\\dist dist /E /I /Y",
    "start-local:unix": "npm run build-frontend:unix && node server.js",
    "build-frontend:unix": "bash -lc 'cd ../frontend && npm install && npm run build && cd ../backend && rm -rf dist && cp -r ../frontend/dist ./dist'",
    "prisma:gen": "npx prisma generate",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "path-to-regexp": "^6.3.0",
    "prisma": "^5.19.1"
  }
}
