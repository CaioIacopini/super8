# MongoDB connection URL (required for Prisma/database)
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/super8?retryWrites=true&w=majority

# JWT Secret (for authentication)
JWT_SECRET=super8-secret-very-strong

# Node environment
NODE_ENV=production

# Port (Render sets this automatically; override if needed)
# PORT=3000
